import { AlertTriangle, LockKeyhole, MapPinOff } from 'lucide-react';
import { Link } from 'react-router-dom';

export default function ErrorState({kind,title,description,retry}:{kind:'404'|'403'|'service';title:string;description:string;retry?:()=>void}){const Icon=kind==='403'?LockKeyhole:kind==='404'?MapPinOff:AlertTriangle;return <section role="alert" className="mx-auto flex min-h-[55vh] max-w-2xl items-center justify-center"><div className="surface-card w-full text-center"><span className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-sky-50 text-sky-700"><Icon className="h-7 w-7"/></span><p className="eyebrow mt-5">{kind==='service'?'Service state':kind}</p><h1 className="mt-2 text-2xl font-black text-primary">{title}</h1><p className="mx-auto mt-2 max-w-lg text-sm leading-6 text-secondary">{description}</p><div className="mt-6 flex flex-wrap justify-center gap-2">{retry&&<button type="button" onClick={retry} className="primary-button">Try again</button>}<Link to="/projects" className="secondary-button">R&amp;D portfolio</Link><Link to="/" className="secondary-button">Return to dashboard</Link></div></div></section>}
